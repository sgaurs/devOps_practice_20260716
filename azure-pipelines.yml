trigger: none

pool:
  name: Aarush_agent_pool

stages:
  - stage: 
    displayName: 
    jobs:
      - job: 
        displayName: 
        steps:
        - task: TerraformInstaller@1
          inputs:
            terraformVersion: 'latest'

        